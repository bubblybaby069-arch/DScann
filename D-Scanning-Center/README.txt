D Scanning Center website

Pages: Home, About, Services, Prices, Contact, FAQ.
Images are self-contained SVG assets, so there are no broken local image paths.
Open index.html to run. Replace SVGs with real machine photos if desired.
